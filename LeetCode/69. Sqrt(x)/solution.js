/**
 * @param {number} x
 * @return {number}
 */
var mySqrt = function(x) {
    //Math.sqrt() → 開根號
    var x = Math.sqrt(x),
    //Math.floor() → 取小於這個數的最大整數
        x = Math.floor(x);
    return x
};